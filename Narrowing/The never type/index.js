"use strict";
exports.__esModule = true;
function print(align) {
    switch (align) {
        case "Left":
            // (parameter) align: "Left"
            console.log(align);
            break;
        case "Right":
            // (parameter) align: "Right"
            console.log(align);
            break;
        default:
            // (parameter) align: never
            console.log(align);
    }
}
