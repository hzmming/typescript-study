"use strict";
exports.__esModule = true;
var Point = /** @class */ (function () {
    function Point(x, y) {
        this.radius = 5; // 给定默认值
        this.type = "circle"; // 只读
        this.x = x;
        this.y = y;
    }
    return Point;
}());
